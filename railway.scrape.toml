"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm cron -- --stages scrape --mode incremental --max-items 500"
cronSchedule = "*/30 * * * *"
restartPolicyType = "NEVER"
