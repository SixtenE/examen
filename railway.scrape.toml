"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm catalog:pipeline -- --skip-embed --skip-seed"
cronSchedule = "0 2 * * *"
restartPolicyType = "NEVER"
