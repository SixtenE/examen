"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm catalog:pipeline"
cronSchedule = "0 3 * * *"
restartPolicyType = "NEVER"
